[package]
name = "latch-compress"
version = "0.2.0"
edition = "2021"
description = "Stateless history compression primitives for Latch"
license = "Apache-2.0"
repository = "https://github.com/EeroEternal/latch"
homepage = "https://github.com/EeroEternal/latch"

[dependencies]
latch-core = { version = "0.2.0", path = "../latch-core" }

[package]
name = "latch-retry"
version = "0.1.0"
edition = "2021"

[dependencies]
latch-core = { path = "../latch-core" }
tokio = { version = "1", features = ["time"], optional = true }

[features]
default = []
tokio = ["dep:tokio"]
